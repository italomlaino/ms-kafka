allprojects  {
  group = 'com.italomlaino'
  version = '0.0.1-SNAPSHOT'

  repositories {
    jcenter()
  }
}

subprojects {
  apply plugin: 'java'
  sourceCompatibility = 1.8
  targetCompatibility = 1.8

  tasks.withType(JavaCompile) {
  	options.encoding = 'UTF-8'
  }
}
